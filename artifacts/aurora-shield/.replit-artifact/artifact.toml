kind = "web"
previewPath = "/"
title = "AuroraShield"
version = "1.0.0"
id = "artifacts/aurora-shield"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26206

[services.development]
run = "pnpm --filter @workspace/aurora-shield run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aurora-shield", "run", "build" ]
publicDir = "artifacts/aurora-shield/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26206"
BASE_PATH = "/"
